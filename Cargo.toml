[package]
name = "rule30"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18.1", features = ["wayland"] }
fastrand = "2.3.0"

[profile.release]
panic = "abort"
strip = true
debug = false
lto = true
incremental = false
codegen-units = 1
